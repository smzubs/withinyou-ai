// src/components/RoadmapDashboard.tsx
// 🎨 Roadmap Dashboard with 60-70% Free Tier Gating
// 💎 Premium features unlocked for paid users
"use client";

import React from "react";

interface DreamLifeData {
  dreamLife: {
    careerTitle: string;
    careerDescription?: string;
    workEnvironment?: string;
    workLifeBalance?: string;
    hollandCode?: string;
    lifestyle?: {
      location: string;
      dailyRoutine: string;
      personalTime: string;
    };
    financial?: {
      targetSalary: string;
      timeline: string;
    };
    ikigaiAnalysis?: {
      passion: string;
      mission: string;
      vocation: string;
      profession: string;
    };
    reasons: string[];
    pathway: string[];
    flowPotential?: string;
  };
  books: Array<{
    title: string;
    author: string;
    rating: number;
    reason: string;
    amazonLink?: string;
    framework?: string;
  }>;
  courses: Array<{
    title: string;
    provider: string;
    instructor?: string;
    reason: string;
    link?: string;
    skillsGained?: string[];
  }>;
  actionPlan: {
    week1: string[];
    week2: string[];
    week3: string[];
    week4: string[];
  };
  mindsetShifts: string[];
  obstacles: Array<{
    obstacle: string;
    solution: string;
    framework?: string;
  }>;
  selfDeterminationTheory?: {
    autonomy: string;
    competence: string;
    relatedness: string;
  };
}

interface RoadmapDashboardProps {
  data: DreamLifeData;
  isPremium: boolean;
  onUpgrade: () => void;
}

export default function RoadmapDashboard({ data, isPremium, onUpgrade }: RoadmapDashboardProps) {
  const { dreamLife, books, courses, actionPlan, mindsetShifts, obstacles } = data;

  // Locked Section Component
  const LockedSection = ({ children, sectionName }: { children: React.ReactNode; sectionName: string }) => (
    <div style={{ position: "relative", marginTop: "1rem" }}>
      <div style={{
        filter: "blur(6px)",
        userSelect: "none",
        pointerEvents: "none",
      }}>
        {children}
      </div>
      <div style={{
        position: "absolute",
        inset: 0,
        background: "linear-gradient(to bottom, rgba(0,0,0,0.5), rgba(0,0,0,0.8))",
        backdropFilter: "blur(2px)",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        gap: "1rem",
        borderRadius: "1.5rem",
      }}>
        <div style={{
          fontSize: "3rem",
          filter: "drop-shadow(0 0 20px rgba(52, 211, 153, 0.6))",
        }}>
          🔒
        </div>
        <p style={{
          fontSize: "1.25rem",
          fontWeight: 600,
          color: "#34d399",
          textAlign: "center",
          textShadow: "0 2px 10px rgba(0,0,0,0.5)",
        }}>
          Unlock {sectionName}
        </p>
        <button
          onClick={onUpgrade}
          style={{
            padding: "0.75rem 2rem",
            fontSize: "1rem",
            fontWeight: 700,
            color: "#ffffff",
            background: "rgba(52, 211, 153, 0.2)",
            backdropFilter: "blur(20px) saturate(180%)",
            border: "1px solid rgba(52, 211, 153, 0.4)",
            borderRadius: "1rem",
            cursor: "pointer",
            transition: "all 0.3s",
            boxShadow: "0 4px 20px rgba(52, 211, 153, 0.3)",
          }}
          onMouseEnter={(e) => {
            e.currentTarget.style.background = "rgba(52, 211, 153, 0.3)";
            e.currentTarget.style.transform = "scale(1.05)";
          }}
          onMouseLeave={(e) => {
            e.currentTarget.style.background = "rgba(52, 211, 153, 0.2)";
            e.currentTarget.style.transform = "scale(1)";
          }}
        >
          Upgrade to Premium
        </button>
      </div>
    </div>
  );

  return (
    <div style={{ maxWidth: "64rem", margin: "0 auto", padding: "2rem 1.5rem" }}>
      
      {/* HEADER */}
      <div style={{
        textAlign: "center",
        marginBottom: "3rem",
      }}>
        <h1 style={{
          fontSize: "2.5rem",
          fontWeight: 700,
          background: "linear-gradient(135deg, #34d399 0%, #22c5c2 50%, #10b981 100%)",
          backgroundClip: "text",
          WebkitBackgroundClip: "text",
          WebkitTextFillColor: "transparent",
          marginBottom: "0.5rem",
        }}>
          ✨ Your Dream Life Roadmap
        </h1>
        <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "1.1rem" }}>
          Scientifically-designed transformation plan just for you
        </p>
      </div>

      {/* ============================================ */}
      {/* DREAM LIFE SECTION - 100% VISIBLE (FREE)     */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          🎯 Your Dream Life
        </h2>

        {/* Career Title & Description */}
        <div style={{ marginBottom: "2rem" }}>
          <h3 style={{
            fontSize: "1.75rem",
            fontWeight: 600,
            color: "rgba(255,255,255,0.95)",
            marginBottom: "0.75rem",
          }}>
            {dreamLife.careerTitle}
          </h3>
          {dreamLife.careerDescription && (
            <p style={{
              fontSize: "1.1rem",
              color: "rgba(255,255,255,0.75)",
              lineHeight: "1.7",
              marginBottom: "1rem",
            }}>
              {dreamLife.careerDescription}
            </p>
          )}
        </div>

        {/* Work Details Grid */}
        <div style={{
          display: "grid",
          gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))",
          gap: "1.5rem",
          marginBottom: "2rem",
        }}>
          {dreamLife.workEnvironment && (
            <div style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.25rem",
              borderRadius: "1rem",
              border: "1px solid rgba(255,255,255,0.1)",
            }}>
              <p style={{ color: "rgba(255,255,255,0.5)", fontSize: "0.875rem", marginBottom: "0.5rem" }}>
                Work Environment
              </p>
              <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1rem", fontWeight: 500 }}>
                {dreamLife.workEnvironment}
              </p>
            </div>
          )}
          {dreamLife.workLifeBalance && (
            <div style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.25rem",
              borderRadius: "1rem",
              border: "1px solid rgba(255,255,255,0.1)",
            }}>
              <p style={{ color: "rgba(255,255,255,0.5)", fontSize: "0.875rem", marginBottom: "0.5rem" }}>
                Work-Life Balance
              </p>
              <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1rem", fontWeight: 500 }}>
                {dreamLife.workLifeBalance}
              </p>
            </div>
          )}
          {dreamLife.hollandCode && (
            <div style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.25rem",
              borderRadius: "1rem",
              border: "1px solid rgba(255,255,255,0.1)",
            }}>
              <p style={{ color: "rgba(255,255,255,0.5)", fontSize: "0.875rem", marginBottom: "0.5rem" }}>
                Holland Code
              </p>
              <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1rem", fontWeight: 500 }}>
                {dreamLife.hollandCode}
              </p>
            </div>
          )}
        </div>

        {/* Lifestyle Section */}
        {dreamLife.lifestyle && (
          <div style={{ marginBottom: "2rem" }}>
            <h4 style={{ color: "#34d399", fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
              🏡 Your Ideal Lifestyle
            </h4>
            <div style={{ display: "flex", flexDirection: "column", gap: "0.75rem" }}>
              <div>
                <span style={{ color: "rgba(255,255,255,0.6)" }}>Location: </span>
                <span style={{ color: "rgba(255,255,255,0.9)" }}>{dreamLife.lifestyle.location}</span>
              </div>
              <div>
                <span style={{ color: "rgba(255,255,255,0.6)" }}>Daily Routine: </span>
                <span style={{ color: "rgba(255,255,255,0.9)" }}>{dreamLife.lifestyle.dailyRoutine}</span>
              </div>
              <div>
                <span style={{ color: "rgba(255,255,255,0.6)" }}>Personal Time: </span>
                <span style={{ color: "rgba(255,255,255,0.9)" }}>{dreamLife.lifestyle.personalTime}</span>
              </div>
            </div>
          </div>
        )}

        {/* Financial Goals */}
        {dreamLife.financial && (
          <div style={{
            background: "rgba(52, 211, 153, 0.1)",
            padding: "1.5rem",
            borderRadius: "1rem",
            border: "1px solid rgba(52, 211, 153, 0.3)",
            marginBottom: "2rem",
          }}>
            <h4 style={{ color: "#34d399", fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
              💰 Financial Freedom
            </h4>
            <div style={{ display: "flex", gap: "3rem", flexWrap: "wrap" }}>
              <div>
                <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "0.875rem", marginBottom: "0.25rem" }}>
                  Target Salary
                </p>
                <p style={{ color: "#34d399", fontSize: "1.5rem", fontWeight: 700 }}>
                  {dreamLife.financial.targetSalary}
                </p>
              </div>
              <div>
                <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "0.875rem", marginBottom: "0.25rem" }}>
                  Timeline
                </p>
                <p style={{ color: "#34d399", fontSize: "1.5rem", fontWeight: 700 }}>
                  {dreamLife.financial.timeline}
                </p>
              </div>
            </div>
          </div>
        )}

        {/* Ikigai Analysis */}
        {dreamLife.ikigaiAnalysis && (
          <div style={{ marginBottom: "2rem" }}>
            <h4 style={{ color: "#34d399", fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
              ⭕ Your Ikigai (Purpose)
            </h4>
            <div style={{
              display: "grid",
              gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))",
              gap: "1rem",
            }}>
              <div style={{ background: "rgba(255,255,255,0.05)", padding: "1rem", borderRadius: "0.75rem" }}>
                <p style={{ color: "#34d399", fontSize: "0.875rem", fontWeight: 600, marginBottom: "0.5rem" }}>
                  Passion
                </p>
                <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "0.95rem" }}>
                  {dreamLife.ikigaiAnalysis.passion}
                </p>
              </div>
              <div style={{ background: "rgba(255,255,255,0.05)", padding: "1rem", borderRadius: "0.75rem" }}>
                <p style={{ color: "#34d399", fontSize: "0.875rem", fontWeight: 600, marginBottom: "0.5rem" }}>
                  Mission
                </p>
                <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "0.95rem" }}>
                  {dreamLife.ikigaiAnalysis.mission}
                </p>
              </div>
              <div style={{ background: "rgba(255,255,255,0.05)", padding: "1rem", borderRadius: "0.75rem" }}>
                <p style={{ color: "#34d399", fontSize: "0.875rem", fontWeight: 600, marginBottom: "0.5rem" }}>
                  Vocation
                </p>
                <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "0.95rem" }}>
                  {dreamLife.ikigaiAnalysis.vocation}
                </p>
              </div>
              <div style={{ background: "rgba(255,255,255,0.05)", padding: "1rem", borderRadius: "0.75rem" }}>
                <p style={{ color: "#34d399", fontSize: "0.875rem", fontWeight: 600, marginBottom: "0.5rem" }}>
                  Profession
                </p>
                <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "0.95rem" }}>
                  {dreamLife.ikigaiAnalysis.profession}
                </p>
              </div>
            </div>
          </div>
        )}

        {/* Why This Fits You */}
        <div style={{ marginBottom: "2rem" }}>
          <h4 style={{ color: "#34d399", fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
            Why This Life Fits YOU
          </h4>
          <div style={{ display: "flex", flexDirection: "column", gap: "0.75rem" }}>
            {dreamLife.reasons.map((reason, idx) => (
              <div key={idx} style={{
                display: "flex",
                alignItems: "flex-start",
                gap: "0.75rem",
              }}>
                <span style={{ color: "#34d399", fontSize: "1.25rem", flexShrink: 0 }}>✓</span>
                <p style={{ color: "rgba(255,255,255,0.85)", fontSize: "1.05rem", lineHeight: "1.6" }}>
                  {reason}
                </p>
              </div>
            ))}
          </div>
        </div>

        {/* Pathway */}
        <div>
          <h4 style={{ color: "#34d399", fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
            🛤️ Your Pathway to This Life
          </h4>
          <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
            {dreamLife.pathway.map((step, idx) => (
              <div key={idx} style={{
                display: "flex",
                alignItems: "flex-start",
                gap: "1rem",
                background: "rgba(255,255,255,0.05)",
                padding: "1rem 1.25rem",
                borderRadius: "0.75rem",
                border: "1px solid rgba(255,255,255,0.1)",
              }}>
                <span style={{
                  background: "linear-gradient(135deg, #34d399 0%, #22c5c2 100%)",
                  color: "#000",
                  fontWeight: 700,
                  fontSize: "0.875rem",
                  width: "2rem",
                  height: "2rem",
                  borderRadius: "50%",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  flexShrink: 0,
                }}>
                  {idx + 1}
                </span>
                <p style={{ color: "rgba(255,255,255,0.85)", fontSize: "1.05rem", lineHeight: "1.6" }}>
                  {step}
                </p>
              </div>
            ))}
          </div>
        </div>

        {/* Flow Potential */}
        {dreamLife.flowPotential && (
          <div style={{
            marginTop: "2rem",
            background: "rgba(52, 211, 153, 0.1)",
            padding: "1.25rem",
            borderRadius: "1rem",
            border: "1px solid rgba(52, 211, 153, 0.3)",
          }}>
            <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "0.875rem", marginBottom: "0.5rem" }}>
              🌊 Flow State Potential
            </p>
            <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1rem", lineHeight: "1.6" }}>
              {dreamLife.flowPotential}
            </p>
          </div>
        )}
      </section>

      {/* ============================================ */}
      {/* BOOKS SECTION - 33% VISIBLE (1 of 3)        */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          📚 Recommended Books
        </h2>

        {/* First Book - VISIBLE */}
        {books[0] && (
          <div style={{
            background: "rgba(255,255,255,0.05)",
            padding: "1.5rem",
            borderRadius: "1.25rem",
            border: "1px solid rgba(255,255,255,0.1)",
            marginBottom: "1.5rem",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "start", marginBottom: "0.75rem" }}>
              <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.4rem", fontWeight: 600 }}>
                {books[0].title}
              </h3>
              <div style={{
                background: "rgba(52, 211, 153, 0.2)",
                padding: "0.25rem 0.75rem",
                borderRadius: "0.5rem",
                border: "1px solid rgba(52, 211, 153, 0.4)",
              }}>
                <span style={{ color: "#34d399", fontSize: "0.95rem", fontWeight: 600 }}>
                  ⭐ {books[0].rating}
                </span>
              </div>
            </div>
            <p style={{ color: "rgba(255,255,255,0.7)", fontSize: "1rem", marginBottom: "0.75rem" }}>
              by {books[0].author}
            </p>
            {books[0].framework && (
              <p style={{
                color: "#34d399",
                fontSize: "0.875rem",
                marginBottom: "0.75rem",
                fontWeight: 500,
              }}>
                Framework: {books[0].framework}
              </p>
            )}
            <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "1.05rem", lineHeight: "1.7", marginBottom: "1rem" }}>
              {books[0].reason}
            </p>
            {books[0].amazonLink && (
              <a
                href={books[0].amazonLink}
                target="_blank"
                rel="noopener noreferrer"
                style={{
                  display: "inline-block",
                  padding: "0.75rem 1.5rem",
                  background: "rgba(52, 211, 153, 0.2)",
                  border: "1px solid rgba(52, 211, 153, 0.4)",
                  borderRadius: "0.75rem",
                  color: "#34d399",
                  fontSize: "1rem",
                  fontWeight: 600,
                  textDecoration: "none",
                  transition: "all 0.3s",
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.background = "rgba(52, 211, 153, 0.3)";
                  e.currentTarget.style.transform = "scale(1.02)";
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.background = "rgba(52, 211, 153, 0.2)";
                  e.currentTarget.style.transform = "scale(1)";
                }}
              >
                View on Amazon →
              </a>
            )}
          </div>
        )}

        {/* Remaining Books - LOCKED (for free users) */}
        {!isPremium && books.length > 1 && (
          <LockedSection sectionName="2 More Book Recommendations">
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              {books.slice(1).map((book, idx) => (
                <div key={idx} style={{
                  background: "rgba(255,255,255,0.05)",
                  padding: "1.5rem",
                  borderRadius: "1.25rem",
                  border: "1px solid rgba(255,255,255,0.1)",
                }}>
                  <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.4rem", fontWeight: 600 }}>
                    {book.title}
                  </h3>
                  <p style={{ color: "rgba(255,255,255,0.7)", fontSize: "1rem" }}>
                    by {book.author}
                  </p>
                </div>
              ))}
            </div>
          </LockedSection>
        )}

        {/* Premium users see all books */}
        {isPremium && books.slice(1).map((book, idx) => (
          <div key={idx} style={{
            background: "rgba(255,255,255,0.05)",
            padding: "1.5rem",
            borderRadius: "1.25rem",
            border: "1px solid rgba(255,255,255,0.1)",
            marginBottom: "1.5rem",
          }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "start", marginBottom: "0.75rem" }}>
              <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.4rem", fontWeight: 600 }}>
                {book.title}
              </h3>
              <div style={{
                background: "rgba(52, 211, 153, 0.2)",
                padding: "0.25rem 0.75rem",
                borderRadius: "0.5rem",
                border: "1px solid rgba(52, 211, 153, 0.4)",
              }}>
                <span style={{ color: "#34d399", fontSize: "0.95rem", fontWeight: 600 }}>
                  ⭐ {book.rating}
                </span>
              </div>
            </div>
            <p style={{ color: "rgba(255,255,255,0.7)", fontSize: "1rem", marginBottom: "0.75rem" }}>
              by {book.author}
            </p>
            {book.framework && (
              <p style={{
                color: "#34d399",
                fontSize: "0.875rem",
                marginBottom: "0.75rem",
                fontWeight: 500,
              }}>
                Framework: {book.framework}
              </p>
            )}
            <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "1.05rem", lineHeight: "1.7", marginBottom: "1rem" }}>
              {book.reason}
            </p>
            {book.amazonLink && (
              <a
                href={book.amazonLink}
                target="_blank"
                rel="noopener noreferrer"
                style={{
                  display: "inline-block",
                  padding: "0.75rem 1.5rem",
                  background: "rgba(52, 211, 153, 0.2)",
                  border: "1px solid rgba(52, 211, 153, 0.4)",
                  borderRadius: "0.75rem",
                  color: "#34d399",
                  fontSize: "1rem",
                  fontWeight: 600,
                  textDecoration: "none",
                  transition: "all 0.3s",
                }}
                onMouseEnter={(e) => {
                  e.currentTarget.style.background = "rgba(52, 211, 153, 0.3)";
                  e.currentTarget.style.transform = "scale(1.02)";
                }}
                onMouseLeave={(e) => {
                  e.currentTarget.style.background = "rgba(52, 211, 153, 0.2)";
                  e.currentTarget.style.transform = "scale(1)";
                }}
              >
                View on Amazon →
              </a>
            )}
          </div>
        ))}
      </section>

      {/* ============================================ */}
      {/* COURSES SECTION - 0% VISIBLE (LOCKED)       */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          🎓 Online Courses
        </h2>

        {!isPremium ? (
          <LockedSection sectionName="3 Personalized Course Recommendations">
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              {courses.map((course, idx) => (
                <div key={idx} style={{
                  background: "rgba(255,255,255,0.05)",
                  padding: "1.5rem",
                  borderRadius: "1.25rem",
                  border: "1px solid rgba(255,255,255,0.1)",
                }}>
                  <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.3rem", fontWeight: 600 }}>
                    {course.title}
                  </h3>
                  <p style={{ color: "rgba(255,255,255,0.7)", fontSize: "0.95rem" }}>
                    {course.provider}
                  </p>
                </div>
              ))}
            </div>
          </LockedSection>
        ) : (
          <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
            {courses.map((course, idx) => (
              <div key={idx} style={{
                background: "rgba(255,255,255,0.05)",
                padding: "1.5rem",
                borderRadius: "1.25rem",
                border: "1px solid rgba(255,255,255,0.1)",
              }}>
                <div style={{ marginBottom: "0.75rem" }}>
                  <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.3rem", fontWeight: 600, marginBottom: "0.25rem" }}>
                    {course.title}
                  </h3>
                  <p style={{ color: "#34d399", fontSize: "0.95rem", fontWeight: 500 }}>
                    {course.provider}{course.instructor && ` • ${course.instructor}`}
                  </p>
                </div>
                <p style={{ color: "rgba(255,255,255,0.8)", fontSize: "1.05rem", lineHeight: "1.7", marginBottom: "1rem" }}>
                  {course.reason}
                </p>
                {course.skillsGained && course.skillsGained.length > 0 && (
                  <div style={{ marginBottom: "1rem" }}>
                    <p style={{ color: "rgba(255,255,255,0.6)", fontSize: "0.875rem", marginBottom: "0.5rem" }}>
                      Skills You'll Gain:
                    </p>
                    <div style={{ display: "flex", flexWrap: "wrap", gap: "0.5rem" }}>
                      {course.skillsGained.map((skill, skillIdx) => (
                        <span key={skillIdx} style={{
                          background: "rgba(52, 211, 153, 0.15)",
                          color: "#34d399",
                          padding: "0.375rem 0.75rem",
                          borderRadius: "0.5rem",
                          fontSize: "0.875rem",
                          fontWeight: 500,
                          border: "1px solid rgba(52, 211, 153, 0.3)",
                        }}>
                          {skill}
                        </span>
                      ))}
                    </div>
                  </div>
                )}
                {course.link && (
                  <a
                    href={course.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    style={{
                      display: "inline-block",
                      padding: "0.75rem 1.5rem",
                      background: "rgba(52, 211, 153, 0.2)",
                      border: "1px solid rgba(52, 211, 153, 0.4)",
                      borderRadius: "0.75rem",
                      color: "#34d399",
                      fontSize: "1rem",
                      fontWeight: 600,
                      textDecoration: "none",
                      transition: "all 0.3s",
                    }}
                    onMouseEnter={(e) => {
                      e.currentTarget.style.background = "rgba(52, 211, 153, 0.3)";
                      e.currentTarget.style.transform = "scale(1.02)";
                    }}
                    onMouseLeave={(e) => {
                      e.currentTarget.style.background = "rgba(52, 211, 153, 0.2)";
                      e.currentTarget.style.transform = "scale(1)";
                    }}
                  >
                    Enroll Now →
                  </a>
                )}
              </div>
            ))}
          </div>
        )}
      </section>

      {/* ============================================ */}
      {/* ACTION PLAN - 50% VISIBLE (Week 1-2)        */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          📅 Your 4-Week Action Plan
        </h2>

        {/* Week 1 - VISIBLE */}
        <div style={{
          background: "rgba(255,255,255,0.05)",
          padding: "1.5rem",
          borderRadius: "1.25rem",
          border: "1px solid rgba(255,255,255,0.1)",
          marginBottom: "1.5rem",
        }}>
          <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600, marginBottom: "1rem" }}>
            💚 Week 1
          </h3>
          <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.75rem" }}>
            {actionPlan.week1.map((task, idx) => (
              <li key={idx} style={{
                display: "flex",
                alignItems: "flex-start",
                gap: "0.75rem",
                color: "rgba(255,255,255,0.85)",
                fontSize: "1.05rem",
                lineHeight: "1.6",
              }}>
                <span style={{ color: "#34d399", fontSize: "1.25rem" }}>•</span>
                {task}
              </li>
            ))}
          </ul>
        </div>

        {/* Week 2 - VISIBLE */}
        <div style={{
          background: "rgba(255,255,255,0.05)",
          padding: "1.5rem",
          borderRadius: "1.25rem",
          border: "1px solid rgba(255,255,255,0.1)",
          marginBottom: "1.5rem",
        }}>
          <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600, marginBottom: "1rem" }}>
            💚 Week 2
          </h3>
          <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.75rem" }}>
            {actionPlan.week2.map((task, idx) => (
              <li key={idx} style={{
                display: "flex",
                alignItems: "flex-start",
                gap: "0.75rem",
                color: "rgba(255,255,255,0.85)",
                fontSize: "1.05rem",
                lineHeight: "1.6",
              }}>
                <span style={{ color: "#34d399", fontSize: "1.25rem" }}>•</span>
                {task}
              </li>
            ))}
          </ul>
        </div>

        {/* Week 3-4 - LOCKED (for free users) */}
        {!isPremium && (
          <LockedSection sectionName="Week 3 & 4 Action Steps">
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              <div style={{
                background: "rgba(255,255,255,0.05)",
                padding: "1.5rem",
                borderRadius: "1.25rem",
                border: "1px solid rgba(255,255,255,0.1)",
              }}>
                <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600 }}>
                  Week 3
                </h3>
              </div>
              <div style={{
                background: "rgba(255,255,255,0.05)",
                padding: "1.5rem",
                borderRadius: "1.25rem",
                border: "1px solid rgba(255,255,255,0.1)",
              }}>
                <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600 }}>
                  Week 4
                </h3>
              </div>
            </div>
          </LockedSection>
        )}

        {/* Premium users see all weeks */}
        {isPremium && (
          <>
            <div style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.5rem",
              borderRadius: "1.25rem",
              border: "1px solid rgba(255,255,255,0.1)",
              marginBottom: "1.5rem",
            }}>
              <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600, marginBottom: "1rem" }}>
                💚 Week 3
              </h3>
              <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.75rem" }}>
                {actionPlan.week3.map((task, idx) => (
                  <li key={idx} style={{
                    display: "flex",
                    alignItems: "flex-start",
                    gap: "0.75rem",
                    color: "rgba(255,255,255,0.85)",
                    fontSize: "1.05rem",
                    lineHeight: "1.6",
                  }}>
                    <span style={{ color: "#34d399", fontSize: "1.25rem" }}>•</span>
                    {task}
                  </li>
                ))}
              </ul>
            </div>

            <div style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.5rem",
              borderRadius: "1.25rem",
              border: "1px solid rgba(255,255,255,0.1)",
            }}>
              <h3 style={{ color: "#34d399", fontSize: "1.3rem", fontWeight: 600, marginBottom: "1rem" }}>
                💚 Week 4
              </h3>
              <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: "0.75rem" }}>
                {actionPlan.week4.map((task, idx) => (
                  <li key={idx} style={{
                    display: "flex",
                    alignItems: "flex-start",
                    gap: "0.75rem",
                    color: "rgba(255,255,255,0.85)",
                    fontSize: "1.05rem",
                    lineHeight: "1.6",
                  }}>
                    <span style={{ color: "#34d399", fontSize: "1.25rem" }}>•</span>
                    {task}
                  </li>
                ))}
              </ul>
            </div>
          </>
        )}
      </section>

      {/* ============================================ */}
      {/* MINDSET SHIFTS - 40% VISIBLE (2 of 5)       */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          💭 Mindset Shifts You Need
        </h2>

        {/* First 2 shifts - VISIBLE */}
        <div style={{ display: "flex", flexDirection: "column", gap: "1rem", marginBottom: "1.5rem" }}>
          {mindsetShifts.slice(0, 2).map((shift, idx) => (
            <div key={idx} style={{
              background: "rgba(255,255,255,0.05)",
              padding: "1.25rem 1.5rem",
              borderRadius: "1rem",
              border: "1px solid rgba(255,255,255,0.1)",
              display: "flex",
              alignItems: "flex-start",
              gap: "1rem",
            }}>
              <span style={{
                background: "linear-gradient(135deg, #34d399 0%, #22c5c2 100%)",
                color: "#000",
                fontWeight: 700,
                fontSize: "0.875rem",
                minWidth: "2rem",
                height: "2rem",
                borderRadius: "50%",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                flexShrink: 0,
              }}>
                {idx + 1}
              </span>
              <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1.05rem", lineHeight: "1.7" }}>
                {shift}
              </p>
            </div>
          ))}
        </div>

        {/* Remaining shifts - LOCKED (for free users) */}
        {!isPremium && mindsetShifts.length > 2 && (
          <LockedSection sectionName={`${mindsetShifts.length - 2} More Critical Mindset Shifts`}>
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              {mindsetShifts.slice(2).map((shift, idx) => (
                <div key={idx} style={{
                  background: "rgba(255,255,255,0.05)",
                  padding: "1.25rem 1.5rem",
                  borderRadius: "1rem",
                  border: "1px solid rgba(255,255,255,0.1)",
                }}>
                  <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1.05rem" }}>
                    {shift}
                  </p>
                </div>
              ))}
            </div>
          </LockedSection>
        )}

        {/* Premium users see all shifts */}
        {isPremium && mindsetShifts.slice(2).map((shift, idx) => (
          <div key={idx} style={{
            background: "rgba(255,255,255,0.05)",
            padding: "1.25rem 1.5rem",
            borderRadius: "1rem",
            border: "1px solid rgba(255,255,255,0.1)",
            display: "flex",
            alignItems: "flex-start",
            gap: "1rem",
            marginBottom: "1rem",
          }}>
            <span style={{
              background: "linear-gradient(135deg, #34d399 0%, #22c5c2 100%)",
              color: "#000",
              fontWeight: 700,
              fontSize: "0.875rem",
              minWidth: "2rem",
              height: "2rem",
              borderRadius: "50%",
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              flexShrink: 0,
            }}>
              {idx + 3}
            </span>
            <p style={{ color: "rgba(255,255,255,0.9)", fontSize: "1.05rem", lineHeight: "1.7" }}>
              {shift}
            </p>
          </div>
        ))}
      </section>

      {/* ============================================ */}
      {/* OBSTACLES - 0% VISIBLE (LOCKED)             */}
      {/* ============================================ */}
      <section style={{
        background: "rgba(52, 211, 153, 0.06)",
        backdropFilter: "blur(40px) saturate(180%)",
        border: "1px solid rgba(52, 211, 153, 0.2)",
        borderRadius: "2rem",
        padding: "2.5rem",
        marginBottom: "2rem",
        boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
      }}>
        <h2 style={{
          fontSize: "2rem",
          fontWeight: 700,
          color: "#34d399",
          marginBottom: "1.5rem",
          display: "flex",
          alignItems: "center",
          gap: "0.75rem",
        }}>
          🚧 Obstacles & Solutions
        </h2>

        {!isPremium ? (
          <LockedSection sectionName="Your Personalized Obstacle Solutions">
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              {obstacles.map((item, idx) => (
                <div key={idx} style={{
                  background: "rgba(255,255,255,0.05)",
                  padding: "1.5rem",
                  borderRadius: "1.25rem",
                  border: "1px solid rgba(255,255,255,0.1)",
                }}>
                  <h3 style={{ color: "rgba(255,255,255,0.95)", fontSize: "1.2rem", fontWeight: 600 }}>
                    {item.obstacle}
                  </h3>
                </div>
              ))}
            </div>
          </LockedSection>
        ) : (
          <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
            {obstacles.map((item, idx) => (
              <div key={idx} style={{
                background: "rgba(255,255,255,0.05)",
                padding: "1.5rem",
                borderRadius: "1.25rem",
                border: "1px solid rgba(255,255,255,0.1)",
              }}>
                <h3 style={{ color: "#ff6b6b", fontSize: "1.2rem", fontWeight: 600, marginBottom: "0.75rem" }}>
                  ⚠️ {item.obstacle}
                </h3>
                {item.framework && (
                  <p style={{
                    color: "#34d399",
                    fontSize: "0.875rem",
                    marginBottom: "0.75rem",
                    fontWeight: 500,
                  }}>
                    Framework: {item.framework}
                  </p>
                )}
                <p style={{ color: "rgba(255,255,255,0.85)", fontSize: "1.05rem", lineHeight: "1.7" }}>
                  <strong style={{ color: "#34d399" }}>Solution:</strong> {item.solution}
                </p>
              </div>
            ))}
          </div>
        )}
      </section>

      {/* FINAL CTA FOR FREE USERS */}
      {!isPremium && (
        <div style={{
          background: "rgba(52, 211, 153, 0.1)",
          backdropFilter: "blur(40px) saturate(180%)",
          border: "1px solid rgba(52, 211, 153, 0.3)",
          borderRadius: "2rem",
          padding: "3rem 2.5rem",
          textAlign: "center",
          boxShadow: "0 20px 60px rgba(0, 0, 0, 0.4)",
        }}>
          <h2 style={{
            fontSize: "2.25rem",
            fontWeight: 700,
            color: "#34d399",
            marginBottom: "1rem",
          }}>
            🚀 Ready to Transform Your Life?
          </h2>
          <p style={{
            color: "rgba(255,255,255,0.8)",
            fontSize: "1.2rem",
            marginBottom: "2rem",
            maxWidth: "600px",
            margin: "0 auto 2rem auto",
          }}>
            Unlock your complete Dream Life roadmap, unlimited sessions, and full transformation toolkit.
          </p>
          <button
            onClick={onUpgrade}
            style={{
              padding: "1.25rem 3rem",
              fontSize: "1.2rem",
              fontWeight: 700,
              color: "#ffffff",
              background: "rgba(52, 211, 153, 0.2)",
              backdropFilter: "blur(20px) saturate(180%)",
              border: "1px solid rgba(52, 211, 153, 0.4)",
              borderRadius: "1.5rem",
              cursor: "pointer",
              transition: "all 0.3s",
              boxShadow: "0 8px 32px rgba(52, 211, 153, 0.3)",
            }}
            onMouseEnter={(e) => {
              e.currentTarget.style.background = "rgba(52, 211, 153, 0.3)";
              e.currentTarget.style.transform = "scale(1.05)";
              e.currentTarget.style.boxShadow = "0 12px 48px rgba(52, 211, 153, 0.5)";
            }}
            onMouseLeave={(e) => {
              e.currentTarget.style.background = "rgba(52, 211, 153, 0.2)";
              e.currentTarget.style.transform = "scale(1)";
              e.currentTarget.style.boxShadow = "0 8px 32px rgba(52, 211, 153, 0.3)";
            }}
          >
            Upgrade to Premium - $14.99/mo
          </button>
        </div>
      )}
    </div>
  );
}